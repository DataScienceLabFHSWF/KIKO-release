# 🌐 Optional ngrok Deployment

This guide is for temporary demos only.

For production deployments, use a proper reverse proxy, HTTPS, authentication, and firewall rules.

## 🚪 What to expose

Only expose:

```text
Frontend
```
